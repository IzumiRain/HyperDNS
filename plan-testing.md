# 🧪 HyperDNS — Comprehensive Testing & Verification Plan

تمامی سناریوها و تست‌های زیر به صورت زنده روی سرور تستی VPS (`95.179.140.241`) با موفقیت ۱۰۰٪ اجرا، تست و وریفای شدند.

---

## 📋 سناریوهای تست قابلیت‌های جدید

### ۱. تست رزولوشن دامنه تست `example.com` (DNS Connectivity Test)
- [x] **حالت ۱ (کلاینت ثبت‌شده و فعال با تاریخ معتبر):**
  - کلاینت آی‌پی خود را ثبت کرده و تاریخ انقضا معتبر است.
  - نتیجه: آی‌پی سرور HyperDNS (`95.179.140.241`) با موفقیت برگردانده شد.
- [x] **حالت ۲ (کاربر ثبت‌نشده / آی‌پی ناشناس):**
  - کاربر آی‌پی خود را ثبت نکرده است.
  - نتیجه: آی‌پی واقعی دامنه `example.com` (کلودفلر: `172.66.147.243` / `104.20.23.154`) برگردانده شد.
- [x] **حالت ۳ (کلاینت منقضی‌شده):**
  - تاریخ انقضای اکانت کلاینت گذشته باشد.
  - نتیجه: اکانت غیرفعال شد و آی‌پی واقعی دامنه `example.com` برگردانده شد.

---

### ۲. تست محدودیت ۱ آی‌پی به ازای هر کلاینت (Strict 1-IP Limit & Replacement)
- [x] **ثبت آی‌پی اول:**
  - کاربر لینک `/ip/{token}` را با IP اول (`10.10.10.10`) ثبت کرد.
  - نتیجه: آی‌پی `10.10.10.10` برای کلاینت ثبت شد.
- [x] **ثبت مجدد با آی‌پی جدید (جایگزینی):**
  - کاربر آی‌پی جدید (`20.20.20.20`) را ثبت کرد.
  - نتیجه: آی‌پی `20.20.20.20` بلافاصله جایگزین شد و فقط ۱ آی‌پی فعال در سیستم ماند.

---

### ۳. تست بررسی انقضا در لحظه ثبت آی‌پی (Accounting Check on Registration)
- [x] **لینک اکانت منقضی‌شده:**
  - کاربر لینکی را باز می‌کند که `ExpiresAt` آن در گذشته است.
  - نتیجه: وضعیت اکانت به `Enabled = false` تغییر یافت و پیام خطای قرمز انقضا نمایش داده شد.

---

### ۴. تست سرویس خودکار بررسی انقضا هر ۱ دقیقه (1-Minute Periodic Expiration Watcher)
- [x] اکانت‌های منقضی‌شده در اسکن خودکار پس‌زمینه شناسایی شده و آی‌پی آن‌ها قطع و اکانت غیرفعال شد.

---

### ۵. تست کامل کنترل دسترسی وایت‌لیست (Whitelist Access Control)
- [x] در حالت Whitelist Mode (`allow_all = false`):
  - آی‌پی‌های ثبت‌نشده خطای `REFUSED` دریافت می‌کنند و دامنه `example.com` باز می‌ماند.

---

### ۶. تست کامل REST API توسعه‌دهندگان و ربات‌ها (`/api/v1/`)
- [x] **تست احراز هویت API Key:**
  - ارسال هدر `X-API-Key: hdns_live_...` ⬅️ نتیجه: `200 OK` دریافت وضعیت زنده سرور.
- [x] **تست ساخت کلاینت از طریق API:**
  - `POST /api/v1/clients` ⬅️ نتیجه: ایجاد اکانت با شناسه یکتا و لینک ثبت آی‌پی.
- [x] **تست تغییر/جایگزینی آی‌پی کلاینت از طریق API:**
  - `POST /api/v1/clients/<ID>/ip` ⬅️ نتیجه: جایگزینی آی‌پی با رعایت محدودیت ۱ آی‌پی.
- [x] **تست تمدید اشتراک کلاینت از طریق API:**
  - `POST /api/v1/clients/<ID>/renew` ⬅️ نتیجه: افزایش روزهای اعتبار.
- [x] **تست دریافت و فیلتر کلاینت‌های منقضی:**
  - `GET /api/v1/clients?status=expired` ⬅️ نتیجه: فیلتر دقیق کاربران منقضی‌شده.
- [x] **تست صفحه مستندات تعاملی API:**
  - `GET /api/v1/docs` ⬅️ نتیجه: صفحه مستندات تعاملی با کد پایتون و ربات تلگرام.

---

### ۷. تست سازگاری و روتینگ ۱۷۱ بازی و سرویس گیمینگ جهانی
- [x] **تست رزولوشن شوترهای تاکتیکال جدید:**
  - `playvalorant.com` ⬅️ `95.179.140.241` (SmartDNS Proxied)
  - `thefinals.com` ⬅️ `95.179.140.241` (SmartDNS Proxied)
  - `escapefromtarkov.com` ⬅️ `95.179.140.241` (SmartDNS Proxied)
  - `playdeltaforce.com` ⬅️ `95.179.140.241` (SmartDNS Proxied)
- [x] **تست بازی‌های انیمه‌ای و MMO:**
  - `genshin.hoyoverse.com` ⬅️ `95.179.140.241` (SmartDNS Proxied)
  - `wutheringwaves.kurogames.com` ⬅️ `95.179.140.241` (SmartDNS Proxied)
  - `pathofexile2.com` ⬅️ `95.179.140.241` (SmartDNS Proxied)
- [x] **تست گیم‌های ریسینگ و فایتینگ:**
  - `assettocorsa.net` ⬅️ `95.179.140.241` (SmartDNS Proxied)
- [x] **تست سرویس‌های کلاود گیمینگ و ابزارها:**
  - `geforcenow.com` ⬅️ `95.179.140.241` (SmartDNS Proxied)
  - `soundcloud.com` ⬅️ `95.179.140.241` (SmartDNS Proxied)

