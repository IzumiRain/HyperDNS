# 🧪 HyperDNS — Comprehensive Testing & Verification Plan

این فایل شامل تمامی سناریوها و تست‌هایی است که پس از آماده‌سازی و پیاده‌سازی قابلیت‌ها، روی سرور یا محیط VPS باید اجرا و وریفای شوند.

---

## 📋 سناریوهای تست قابلیت‌های جدید

### ۱. تست رزولوشن دامنه تست `example.com` (DNS Connectivity Test)
- [ ] **حالت ۱ (کلاینت ثبت‌شده و فعال با تاریخ معتبر):**
  - کلاینت آی‌پی خود را ثبت کرده و تاریخ انقضا معتبر است.
  - دستور `nslookup example.com <DNS_SERVER_IP>` یا `dig @<DNS_SERVER_IP> example.com A` اجرا شود.
  - **نتیجه مورد انتظار:** آی‌پی سرور HyperDNS (Public IP) برگردانده شود.
- [ ] **حالت ۲ (کاربر ثبت‌نشده / آی‌پی ناشناس):**
  - کاربر آی‌پی خود را ثبت نکرده است.
  - دستور `nslookup example.com <DNS_SERVER_IP>` اجرا شود.
  - **نتیجه مورد انتظار:** آی‌پی واقعی دامنه `example.com` (کلودفلر: `93.184.215.14` یا آپ‌استریم) برگردانده شود.
- [ ] **حالت ۳ (کلاینت منقضی‌شده):**
  - تاریخ انقضای اکانت کلاینت گذشته باشد.
  - دستور `nslookup example.com <DNS_SERVER_IP>` اجرا شود.
  - **نتیجه مورد انتظار:** آی‌پی واقعی دامنه `example.com` برگردانده شود.

---

### ۲. تست محدودیت ۱ آی‌پی به ازای هر کلاینت (Strict 1-IP Limit & Replacement)
- [ ] **ثبت آی‌پی اول:**
  - کاربر لینک `/ip/{token}` را با IP اول (مثلاً `1.1.1.1`) باز می‌کند.
  - **نتیجه:** آی‌پی `1.1.1.1` برای کلاینت ثبت می‌شود.
- [ ] **ثبت مجدد با آی‌پی جدید (جایگزینی):**
  - کاربر لینک `/ip/{token}` را با IP دوم (مثلاً `2.2.2.2`) باز می‌کند.
  - **نتیجه:** آی‌پی `2.2.2.2` جایگزین `1.1.1.1` می‌شود و در سیستم دقیقاً ۱ آی‌پی فعال می‌ماند. آی‌پی قبلی غیرفعال می‌شود.
- [ ] **ثبت مجدد با همان آی‌پی (تکراری):**
  - کاربر لینک `/ip/{token}` را دوباره با همان IP باز می‌کند.
  - **نتیجه:** پیام تأیید ثبت قبلی نشان داده می‌شود و تغییری در اکانت ایجاد نمی‌شود.

---

### ۳. تست بررسی انقضا در لحظه ثبت آی‌پی (Accounting Check on Registration)
- [ ] **لینک اکانت منقضی‌شده:**
  - کاربر لینکی را باز می‌کند که `ExpiresAt` آن در گذشته است.
  - **نتیجه:**
    - وضعیت اکانت به `Enabled = false` تغییر کند.
    - پیام خطای قرمز: "اعتبار اشتراک شما به پایان رسیده است — اکانت غیرفعال شد" نمایش داده شود.
    - هیچ آی‌پی‌ای برای این اکانت فعال نماند.

---

### ۴. تست سرویس خودکار بررسی انقضا هر ۱ دقیقه (1-Minute Periodic Expiration Watcher)
- [ ] یک اکانت تستی با زمان انقضای ۲ دقیقه ساخته شود.
- [ ] بعد از گذشت زمان انقضا، بدون هیچ درخواستی، بررسی شود که:
  - لاگ `[Accounting] Client expired. Account deactivated` ثبت شده باشد.
  - وضعیت اکانت به `Enabled = false` درآمده باشد.
  - کوئری‌های DNS این آی‌پی بلافاصله قطع شوند (`REFUSED`).

---

### ۵. تست کامل کنترل دسترسی وایت‌لیست (Whitelist Access Control)
- [ ] در حالت Whitelist Mode (`allow_all = false`):
  - آی‌پی‌های ثبت‌نشده برای دامنه‌های عادی خطای `REFUSED` دریافت کنند.
  - برای دامنه `example.com` آی‌پی واقعی برگردد (جهت تست اتصال).
  - پس از ثبت آی‌پی، تمام دامنه‌ها و گیم‌ها متصل و پروکسی شوند.
